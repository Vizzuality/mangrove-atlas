/**
 * Privacy Notice supplied by the client (GMW-1073). Served as a downloadable PDF from
 * `public/documents`; swap this for an external URL if the client hosts it elsewhere.
 * Link to it with a plain <a download> — next/link would try to client-route the path.
 */
export const PRIVACY_NOTICE_URL = '/documents/privacy-notice.pdf';
